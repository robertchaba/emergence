{
  "name": "emergence",
  "version": "0.1.0",
  "private": true,
  "license": "BSD-3-Clause",
  "description": "A browser-based emergent evolution sandbox.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "npm run test:headless && playwright test",
    "test:headless": "node --test tests/headless/*.test.js tests/rendering.test.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "vite": "8.3.0"
  }
}
